 * update examples, since they use the osgwrapper we removed from here.
 * add boost requirements to private libs in pkg-config file
 * any method that can throw an exception should specify its throws
 * Add CMakeList.txt files from the original osgIntrospection project so that it still works.
